version: "3.8"

services:
  redis:
    container_name: redis
    image: redis
    ports:
      - "6379:6379"
    restart: always

  application:
    container_name: app
    ports:
      - "8080:8080"
    depends_on:
      - redis
    restart: always